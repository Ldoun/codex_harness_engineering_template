interface:
  display_name: "Doc Consistency Check"
  short_description: "Check consistency across repo docs, including UX"
  default_prompt: "Use $doc-consistency-check to review product, engineering, UX, plan, and the active spec for consistency."
