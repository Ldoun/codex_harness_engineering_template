interface:
  display_name: "Doc Consistency Check"
  short_description: "Check consistency across repo docs"
  default_prompt: "Use $doc-consistency-check to review product, engineering, plan, and the active spec for consistency."
