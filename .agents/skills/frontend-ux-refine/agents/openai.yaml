interface:
  display_name: "Frontend UX Refine"
  short_description: "Improve an existing UI in place while preserving the design system"
  default_prompt: "Use $frontend-ux-refine to improve spacing, hierarchy, responsiveness, state clarity, and accessibility on an existing UI without inventing a new art direction."
policy:
  allow_implicit_invocation: true
