# Feature Spec

Status: proposed
Valid values: proposed | in progress | complete | removed

Use this file as a scoped working contract for a non-trivial change.
Keep it narrow, practical, tied to current implementation work, and limited to one coherent behavior change.
Stable architecture and shared constraints belong in `docs/engineering.md`, not here.
Use `docs/specs/_iterative-template.md` instead when the goal is a broad, eval-driven refinement loop rather than one bounded behavior change.

## Summary
- Feature or task:
- Why this change exists:

## Scope
- In scope:
- Out of scope:

## Behavior Requirements
- Required behavior:
- Edge or failure cases:
- Feature-specific fixture or test-data needs:

## UX / Visual Requirements
- Visual input source type:
- Source references:
- Canonical source of truth:
- Existing components or tokens to reuse:
- Required states:
- Required breakpoints:
- Accessibility requirements:
- Notes on ambiguity or repo-convention overrides:

## Acceptance Criteria
- Observable result 1:
- Observable result 2:

## Test Plan
- Tests to add or update before implementation:
- Expected failing coverage before implementation, when practical:
- Manual verification, if still needed:
- Commands or environments to use:
- If TDD is skipped, reason and equivalent validation:

## Visual Validation
- Visual-input artifact path:
- Screenshot / Figma / Storybook references:
- Automated checks:
- Manual comparison steps:

## Open Questions
- Only unresolved questions that block or materially affect implementation:
